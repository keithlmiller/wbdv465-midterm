= form_for @user_book do |f|
  - if @user_book.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_book.errors.count, "error")} prohibited this user_book from being saved:"
      %ul
        - @user_book.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
